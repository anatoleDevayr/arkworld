<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mundoarkano.IncidentesInternos"
             BackgroundColor="White">
  <ContentPage.Padding>
    <OnPlatform x:TypeArguments="Thickness"
                iOS="20, 40, 20, 20"
                Android="20, 20, 20, 20"
                WinPhone="20, 20, 20, 20" />
  </ContentPage.Padding>
  <ContentPage.Content>
    <ScrollView>
      <StackLayout VerticalOptions="Fill"
                   HorizontalOptions="Fill"
                   Orientation="Vertical"
                   Spacing="30"
                   BackgroundColor="White">
        <Label Text="Reporte incidentes internos" HorizontalOptions="Center" FontSize="15" TextColor="Black" />
        <Label Text="Detalle su incidente: " HorizontalOptions="Center" FontSize="15" TextColor="Black" />
        <Editor x:Name="editIncidenteII" HeightRequest="200" BackgroundColor="Gray">
        </Editor>
        <Button x:Name="btnEnviarIncidenteII" Text="Enviar" IsEnabled="True" Clicked="OnEnviarIncidenteII" BackgroundColor="Gray" BorderRadius="5" BorderColor="Transparent" />

      </StackLayout>
    </ScrollView>
  </ContentPage.Content>
</ContentPage>